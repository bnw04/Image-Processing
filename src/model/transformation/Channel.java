package model.transformation;

/**
 * The Channel enum represents the color channels of an image pixel.
 * It is used to specify which color channel (RED, GREEN, or BLUE)
 * is being accessed or modified when working with transformation
 * of the image objects.
 */
public enum Channel {
  RED,
  GREEN,
  BLUE
}
